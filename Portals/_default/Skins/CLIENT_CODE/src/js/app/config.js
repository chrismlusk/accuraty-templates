// 1a. Import components needed globally.
import Header from '../components/Header';
import NavMenu from '../components/NavMenu';

// 1b. Add those components to this object.
export const GlobalComponents = {
  Header,
  NavMenu,
};

// 2. The data-attributes used in DOM elements.
export const DataAttribute = {
  COMPONENT: 'data-component',
  COMPONENT_OPTIONS: 'data-component-options',
};
